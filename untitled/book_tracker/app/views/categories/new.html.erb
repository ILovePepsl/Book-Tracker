<h1>New Category</h1>
<%= form_for @category do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Create Category' %>
  </div>
<% end %>
<%= link_to 'Back to New Book', new_book_path %>
