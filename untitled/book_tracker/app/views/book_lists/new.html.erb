<h1>New Book List</h1>

<%= form_with model: @book_list do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Create Book List' %>
  </div>
<% end %>

<%= link_to 'Back to Lists', book_lists_path %>
